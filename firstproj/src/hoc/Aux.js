
const aux = props => props.children;

export default aux;


//wraps other component. No logic or styling. Just wraps component, maybe adding extra logic at times. This is a Higher Order Component(HOC)